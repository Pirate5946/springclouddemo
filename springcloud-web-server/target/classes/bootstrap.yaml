server:
  port: 8088

# 服务与服务之间相互调用一般都是根据这个name 。
spring:
  application:
    name: springcloud-web-server

eureka:
  client:
    serviceUrl:
      #      指定服务注册中心的地址
      defaultZone: http://localhost:8061/eureka/

#启用hystrix
feign:
  hystrix:
    enabled: true
